# Full-Stack Software Specification — Notes App

Generated by the codebase-modeler skill. Every claim cites the code it is derived from.

## 1. Executive Summary
* **Project Name:** Notes App
* **Version:** 1.0
* **Purpose:** A minimal REST API for storing user-owned notes.
* **Target Audience:** API clients.

## 2. Project Scope
* **In-Scope:** login (`routes/auth.py:9-15`), note create/read (`routes/notes.py:9-21`),
  two-table SQLite schema (`migrations/0001_init.sql:1-18`).
* **Out-of-Scope:** list/delete endpoints, sessions, roles, any frontend.
* **Assumptions & Constraints:** single-process dev server only (`app.py:17-18`).

## 3. System Architecture & Technology Stack
* **3.1 High-Level Architecture:** HTTP client -> Flask app (`app.py:7`) -> two Blueprints
  (`app.py:13-14`) -> Flask-SQLAlchemy (`db.py:4`) -> SQLite file DB (`app.py:8-9`).
* **3.2 Technology Stack:**
  * **Frontend:** none in repo (UNVERIFIED — no template/SPA sources).
  * **Backend:** Python + Flask 3.0.0 (`requirements.txt:1`), Blueprints (`routes/auth.py:6`).
  * **Database:** SQLite via Flask-SQLAlchemy 3.1.1 (`requirements.txt:2`), schema
    `migrations/0001_init.sql:1-18`.
  * **Infrastructure & DevOps:** none — dev-only `app.run(debug=True)` (`app.py:18`), no
    Dockerfile or CI config in repo.

## 4. Functional Requirements
| ID | Requirement | Evidence |
| :-- | :-- | :-- |
| FR-1 | Authenticate user by email + password; `401` otherwise | `routes/auth.py:9-15` |
| FR-2 | Create a note from JSON payload; `201` + new id | `routes/notes.py:9-15` |
| FR-3 | Fetch a note by id; `200` or `404` | `routes/notes.py:18-21` |
| FR-4 | Hash passwords at rest | `models.py:16-20` |
| FR-5 | Notes owned by users (1:N FK) | `models.py:14`, `models.py:26` |

* **User Authentication & Authorization:** credential login only, bare user id/email on
  success (`routes/auth.py:15`).

## 5. Non-Functional Requirements (NFRs)
* **Performance:** no targets stated in repo; single index on `notes(user_id)` suggests
  the list-by-user hot path (`migrations/0001_init.sql:18`).
* **Security:** passwords hashed (`models.py:16-20`); `debug=True` in the dev entrypoint is
  an anti-pattern for any non-local deployment (`app.py:18`).
* **Error handling:** `create_note` raises an unhandled `KeyError` -> raw `500` when the
  payload lacks `user_id` (`routes/notes.py:12`).

## 6. Data Model & Database Schema
* **Entities & Relationships:** `User` (`models.py:7-20`) 1:N owns `Note`
  (`models.py:23-29`) via `notes.user_id` FK (`migrations/0001_init.sql:12`).

## 7. API Specification
* **Protocol:** REST over HTTP, JSON payloads.
* **Endpoints:**

| Method | Path | Handler | Success | Error | Evidence |
| :-- | :-- | :-- | :-- | :-- | :-- |
| POST | `/api/login` | `login` | `200 {id, email}` | `401 {error}` | `routes/auth.py:9-15` |
| POST | `/api/notes` | `create_note` | `201 {id}` | unhandled `KeyError` -> `500` | `routes/notes.py:9-15` |
| GET | `/api/notes/<int:note_id>` | `get_note` | `200` | `404` | `routes/notes.py:18-21` |

## 8. UI/UX & Design
* **Design System:** none — no frontend in repo.
* **User Flows:** login then create note (see ActivityDiagram.md, PR-1 / PR-2).

## 9. Testing Strategy
* **Unit Testing:** no test suite in repo — UNVERIFIED (no `tests/`, no pytest dep in
  `requirements.txt:1-2`).
* **End-to-End (E2E) Testing:** none present.

## 10. Deployment & Milestones
* **Environments:** dev only — `app.run(debug=True)` (`app.py:17-18`); no staging/production
  artifacts.
* **Timeline & Phases:** Phase 1 (implemented): login + note create/read.
