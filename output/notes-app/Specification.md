# Full-Stack Software Specification — Notes App

Generated by the codebase-modeler skill. Every claim cites the code it is derived
from (`path:line` ranges) so the specification can be verified against the
repository at the pinned tree.

## 1. Executive Summary
* **Project Name:** Notes App
* **Version:** 1.0
* **Author(s):** UNVERIFIED (no author metadata in repo)
* **Date:** UNVERIFIED (no version metadata in repo)
* **Purpose:** A minimal REST API for storing user-owned notes: users log in and
  create notes, which are persisted to a relational database. Entry point:
  `app.py:7-10`.
* **Target Audience:** API clients (no in-repo UI — see §8).

## 2. Project Scope
* **In-Scope:**
  * User login with email + password (`routes/auth.py:9-15`).
  * Create a note (POST) and fetch a single note (GET) (`routes/notes.py:9-21`).
  * Two-table relational schema with one index (`migrations/0001_init.sql:1-18`).
* **Out-of-Scope:** list/delete note endpoints, token-based sessions, role-based
  access, any frontend — none present (UNVERIFIED by absence search).
* **Assumptions & Constraints:** single-process dev server only
  (`app.py:17-18`); passwords hashed with Werkzeug (`models.py:4`).

## 3. System Architecture & Technology Stack
* **3.1 High-Level Architecture:** Browser/HTTP client -> Flask app
  (`app.py:7`) -> two Blueprints registered at `app.py:13-14` ->
  Flask-SQLAlchemy (`db.py:4`) -> SQLite file DB (`app.py:8-9`).
* **3.2 Technology Stack:**
  * **Frontend:** none in repo (UNVERIFIED — no template/SPA sources).
  * **Backend:** Python + Flask 3.0.0 (`requirements.txt:1`), Blueprints
    (`routes/auth.py:6`, `routes/notes.py:6`).
  * **Database:** SQLite via Flask-SQLAlchemy 3.1.1 (`requirements.txt:2`),
    URI from `DATABASE_URL` env with `sqlite:///notes.db` default
    (`app.py:8-9`); schema `migrations/0001_init.sql:1-18`.
  * **Data Processing & Analytics:** none.
  * **Infrastructure & DevOps:** none — no Dockerfile/CI found (UNVERIFIED);
    dev-only `app.run(debug=True)` (`app.py:18`).

## 4. Functional Requirements
| ID | Requirement | Evidence |
| :-- | :-- | :-- |
| FR-1 | Authenticate a user by email + password; return user id + email on success, `401` otherwise | `routes/auth.py:9-15` |
| FR-2 | Create a note for a user from a JSON payload (user_id, title, body); return `201` + new id | `routes/notes.py:9-15` |
| FR-3 | Fetch a note by id; return `200` with id/title/body or `404` | `routes/notes.py:18-21` |
| FR-4 | Hash passwords at rest (generate + verify) | `models.py:16-20` |
| FR-5 | Keep notes owned by users (1:N, `notes.user_id` FK) | `models.py:14`, `models.py:26` |

* **User Authentication & Authorization:** credential login only; no tokens or
  roles — the response to a successful login is a bare user id/email
  (`routes/auth.py:15`).
* **Core Features:** FR-1..FR-5 above; each endpoint's acceptance criteria are its
  success/error status codes as cited.
* **Third-Party Integrations:** none (UNVERIFIED — no SDK deps in
  `requirements.txt:1-2`).

## 5. Non-Functional Requirements (NFRs)
* **Performance:** UNVERIFIED (no benchmark targets or query profiling in repo);
  single index on `notes(user_id)` suggests list-by-user access
  (`migrations/0001_init.sql:18`).
* **Scalability:** UNVERIFIED — single dev server (`app.py:18`), SQLite
  file-based storage (`app.py:9`).
* **Security:** passwords hashed, never stored raw (`models.py:11`,
  `models.py:16-20`); email unique (`models.py:10`); `debug=True` in the dev
  entrypoint is an anti-pattern for any non-local deployment (`app.py:18`).
* **Reliability & Availability:** UNVERIFIED (no health endpoints, retries, or
  uptime targets found).

## 6. Data Model & Database Schema
* **Entities & Relationships:** `User` (`models.py:7-20`) 1:N owns `Note`
  (`models.py:23-29`) via `notes.user_id` FK (`models.py:26`,
  `migrations/0001_init.sql:12`). See `DatabaseModel.md` for the full ERD.
* **Data Flow:** JSON body -> `request.get_json` -> ORM instance ->
  `db.session.add` / `commit` -> JSON response (`routes/notes.py:11-15`).

## 7. API Specification
* **Protocol:** REST over HTTP, JSON payloads (no OpenAPI doc — UNVERIFIED).
* **Endpoints:**

| Method | Path | Handler | Success | Error | Evidence |
| :-- | :-- | :-- | :-- | :-- | :-- |
| POST | `/api/login` | `login` | `200 {id, email}` | `401 {error}` | `routes/auth.py:9-15` |
| POST | `/api/notes` | `create_note` | `201 {id}` | unhandled `KeyError` -> `500` | `routes/notes.py:9-15` |
| GET | `/api/notes/<int:note_id>` | `get_note` | `200 {id, title, body}` | `404` | `routes/notes.py:18-21` |

## 8. UI/UX & Design
* **Design System:** none — no frontend in repo (UNVERIFIED).
* **User Flows:** login then create note, as modeled in `ActivityDiagram.md`
  (processes PR-1 / PR-2).
* **Wireframes/Mockups:** none (UNVERIFIED).

## 9. Testing Strategy
* **Unit Testing:** no test suite in repo — UNVERIFIED (no `tests/`,
  `pytest` deps in `requirements.txt:1-2`).
* **Integration Testing:** none present — UNVERIFIED.
* **End-to-End (E2E) Testing:** none present — UNVERIFIED.

## 10. Deployment & Milestones
* **Environments:** dev only — `app.run(debug=True)` (`app.py:17-18`); no
  staging/production artifacts (UNVERIFIED: no Dockerfile, no CI config).
* **Timeline & Phases:** UNVERIFIED (no project-management docs in repo).
  * Phase 1 (implemented): login + note create/read (this codebase).
  * Phase 2 (suggested, out of scope): note list/delete, sessions/tokens.
  * Phase 3 (suggested, out of scope): production deployment with a real web
    server (gunicorn/uWSGI) replacing `app.py:18`.
